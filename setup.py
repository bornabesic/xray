
from setuptools import setup, find_packages

setup(
    name="xray",
    author="Borna Bešić",
    description="Monitor function's locals from outside in parallel with its execution",
    packages=find_packages(),
)

